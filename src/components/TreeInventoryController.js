import React from 'react';
import PropTypes from 'prop-types';

const TreeInventoryController = props => {
  return (
    <div>TreeInventoryController</div>
  )
}

TreeInventoryController.propTypes = {}

export default TreeInventoryController